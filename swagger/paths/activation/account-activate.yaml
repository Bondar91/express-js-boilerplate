patch:
  summary: Account Activation
  operationId: accountActivation
  tags:
    - activation
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas/activation/account-activation-request.yaml"
  responses:
    "200":
      description: Account activation successful
      content:
        application/json:
          schema:
            $ref: "../../schemas/common/response.yaml"
    "400":
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "../../schemas/common/error-400.yaml"
    "500":
      description: Server error
      content:
        application/json:
          schema:
            $ref: "../../schemas/common/error-default.yaml"
