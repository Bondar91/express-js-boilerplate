post:
  summary: Forgot Password user
  operationId: forgotPassword
  tags:
    - auth
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas/auth/forgot-password-request.yaml"
  responses:
    "200":
      description: Forgot password successful
      content:
        application/json:
          schema:
            $ref: "../../schemas/common/response.yaml"
    "400":
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "../../schemas/common/error-400.yaml"
    "500":
      description: Server error
      content:
        application/json:
          schema:
            $ref: "../../schemas/common/error-default.yaml"
